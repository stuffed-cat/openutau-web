# OpenUtau WebUI AI Assistant Instructions
无论如何都必须遵守 "不可以使用git不可以使用任何方式使用git不可以使用任何命令/脚本修改代码只能使用工具,不可以修改submodules,不可能是缓存问题,不可以杀进程,不要以任何方案清空会暗示缓存有问题,不可以用任何方式建议/暗示用户清理缓存, 不可以使用任何方式清理任何缓存.不可以以任何方式修改编译产物, 不可以使用任何的mock和stub fake todo，不可以进行任何投机，倒把骗过用户的行为坐在你面前的是一个计算机行业的专家，不要做给你面子，不接受必须直接点名，还要死鸭子嘴硬，必须遵守规则，不要不给面子才动"规则不容忍任何变通,任何变通就会比死亡还痛苦的惩罚

1. 不要尝试让openutau core在web 环境下运行，除非你想被打死。
2. 劫持openutau.core的namespace把类重新写为调用(通过backend项目提供的API)